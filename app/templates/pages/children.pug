include ../components/head-block
include ../mixins/banner

.section.section_space_half
	h1.title Дети

	+head-block(
		smData= banner_sm_children
		lgData= banner_lg_children
		textData= head_block__last_children
	)

.section
	.container
		.main__space_bot-half
			each val in banner_children
				+banner(val)
		.text-center
			a.btn(href="#") Заказать визит