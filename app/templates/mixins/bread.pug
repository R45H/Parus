mixin bread(data)
	ul.bread(class= attributes.class)

		li.bread__item
			a.bread__link(href= '/') Главная

		each val in menu

			if val.link == page.link
				li.bread__item
					span.bread__text= val.text